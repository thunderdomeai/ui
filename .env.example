# Backend: optional GitHub token to inject into window.__UNIFIED_UI_CONFIG__ for branch/env fetches
DEFAULT_GITHUB_TOKEN=
GITHUB_TOKEN=

# Credential store persistence (GCS). If unset, falls back to /tmp for local dev.
# CREDENTIALS_BUCKET=
# CREDENTIALS_PREFIX=unified-ui-credentials
# CREDENTIALS_BUCKET_LOCATION=US

# Optional: Override downstream services if needed
# MAIN_API_URL=
# MCP_REGISTRY_BASE_URL=
# THUNDERDEPLOY_BASE_URL=
# TRIGGERSERVICE_BASE_URL=
# AGENT_REGISTRY_BASE_URL=
